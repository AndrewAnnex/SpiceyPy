sudo: false
language: python
cache: pip
python:
 - "2.7"
 - "3.3"
 - "3.4"
install:
    - "pip install -r requirements.txt"
    - "python setup.py install"
# run tests
script:
    - coverage run --source=SpiceyPy setup.py test
after_success:
    - coveralls